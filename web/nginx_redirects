#if ($http_host != 'new.axioma.pro') {
#    return 301 http://new.axioma.pro$request_uri;
#}    

    if ( $request_url ~ /catalog/etiketka_mini-ultra-strip_2$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /category/schyotchiki-megacount$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /category/schyotchiki-smcounter $) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /catalog/etiketki_40x40_mm_lucatron$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /catalog/kovrik$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /catalog/protivokrajnaya_sistema__ams_1140$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /catalog/spectra$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /catalog/deaktivator_radiochastotny$ ) {
        rewrite ^ /catalog permanent;
    }
    if ( $request_url ~ /catalog/gvozdik$ ) {
        rewrite ^ /catalog permanent;
    }
    if ( $request_url ~ /catalog/syemniki_standart$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_url ~ /kontakti$ ) {
        rewrite ^ /kontaktyi permanent;
    }
