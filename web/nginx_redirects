#if ($http_host != 'new.axioma.pro') {
#    return 301 http://new.axioma.pro$request_uri;
#}    

#    if ( $request_uri ~ ^/catalog/etiketka_mini-ultra-strip_2$ ) {
#        rewrite ^ /catalog permanent;
#    }

    if ( $request_uri ~ ^/category/schyotchiki-megacount$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/category/schyotchiki-smcounter$) {
        rewrite ^ /catalog permanent;
    }

#    if ( $request_uri ~ ^/catalog/etiketki_40x40_mm_lucatron$ ) {
#        rewrite ^ /catalog permanent;
#    }

    if ( $request_uri ~ ^/catalog/kovrik$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/catalog/protivokrajnaya_sistema__ams_1140$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/catalog/spectra$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/catalog/deaktivator_radiochastotny$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/catalog/syemniki_standart$ ) {
        rewrite ^ /catalog permanent;
    }

    if ( $request_uri ~ ^/kontakti$ ) {
        rewrite ^ /kontaktyi permanent;
    }

    if ( $request_uri ~ ^/deaktivatoryi_etiketok$ ) {
        rewrite ^ /category/deaktivatoryi_etiketok permanent;
    }

    if ( $request_uri ~ ^/politic$ ) {
        rewrite ^ /soglasie permanent;
    }

    if ( $request_uri ~ ^/catalog/gvozdik$ ) {
        rewrite ^ /catalog/gvozdik-igla-super-tag permanent;
    }

    if ( $request_uri ~ ^/videonabludenie$ ) {
        rewrite ^ /category/videonabludenie permanent;
    }

    if ( $request_uri ~ ^/catalog/gvozdik_AM%2019$ ) {
        rewrite ^ /catalog/gvozdik_pencil_tag_19 permanent;
    }

if ( $request_uri ~ ^/catalog/megacount_stereo_stereocount3d$) {
        rewrite ^ /catalog/3d-schetchik-posetitelej-stereocount3d permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i102$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-kupolnaya-ds-i102 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i1202$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-kupolnaya-ds-i202 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i402$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-kupolnaya-ds-i402 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i100$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-ds-i100 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i200-b-$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-ds-i200b permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-i400$) {
        rewrite ^ /catalog/ip-videokamera-hiwatch-ds-i400 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-n316-b-$) {
        rewrite ^ /catalog/ip-registrator-ds-n316b permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-n204-b-$) {
        rewrite ^ /catalog/ip-registrator-ds-n204b permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-n204p-b-$) {
        rewrite ^ /catalog/ip-registrator-ds-n204pb permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-n308-b-$) {
        rewrite ^ /catalog/ip-registrator-ds-n308b permanent;
	}
    if ( $request_uri ~ ^/catalog/mk-395$) {
        rewrite ^ /catalog/avtomaticheskij-semnik-power-detacher-mk-395 permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitny_can_tag$) {
        rewrite ^ /catalog/akustomagnitnaya-birka-can-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitny_coffee_tag$) {
        rewrite ^ /catalog/akustomagnitnaya-birka-milk-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/zhestkij_datchik_pensil_tag$) {
        rewrite ^ /catalog/akustomagnitnaya-birka-pencil-tag-mini permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitny_optical_tag$) {
        rewrite ^ /catalog/akustomagnitnaya-birka-optical-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/synergy_1170$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-synergy-1170 permanent;
	}
    if ( $request_uri ~ ^/catalog/synergy_1175$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-synergy-1175 permanent;
	}
    if ( $request_uri ~ ^/catalog/synergy_1180$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-synergy-1180 permanent;
	}
    if ( $request_uri ~ ^/catalog/protivokrajnaya_sistema_ultraexit$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-ultra-exit permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitnaya-sistema-sensormatic-ultra-post-vi-$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-ultra-post-vi permanent;
	}
    if ( $request_uri ~ ^/catalog/protivokrajnaya_sistema_ultrashield$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-sensormatic-ultra-shield permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitnaya-sistema-synergy-1175-2-5-m-$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-synergy-1175 permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitnaya-sistema-synergy-2-5-acrylic$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-synergy-acrylic permanent;
	}
    if ( $request_uri ~ ^/catalog/akustomagnitnaya-sistema-sensormatic-ultra-1-8-m-$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-ultra-exit permanent;
	}
    if ( $request_uri ~ ^/catalog/komlekt_iz_2_antenn_ultra_shield$) {
        rewrite ^ /catalog/akustomagnitnaya-sistema-ultra-shield permanent;
	}
    if ( $request_uri ~ ^/catalog/mus-2-le$) {
        rewrite ^ /catalog/akustomagnitnaya-ehtiketka-sensormatic-mus-ii permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketka_mus_3_dr$) {
        rewrite ^ /catalog/akustomagnitnaya-ehtiketka-sensormatic-mus-iii permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketka_mini-ultra-strip_3$) {
        rewrite ^ /catalog/akustomagnitnaya-ehtiketka-dvuhkonturnaya permanent;
	}
    if ( $request_uri ~ ^/catalog/schetchik_posetiteley_megacount_wireless_rc_eth$) {
        rewrite ^ /catalog/komplekt-schetchikov-rc-eth-s-ethernet permanent;
	}
    if ( $request_uri ~ ^/catalog/schetchik_posetiteley_megacount_wireless_rc-wifi$) {
        rewrite ^ /catalog/komplekt-schetchikov-rc-wifi-s-ethernet-wifi permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-akustomagnitnyj-pencil-tag-midi-$) {
        rewrite ^ /catalog/birka-akustomagnitnaya-pencil-tag-midi-ferrit-33-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-butylochnyj-am-bottle-tag-$) {
        rewrite ^ /catalog/birka-akustomagnitnaya-bottle-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/blok-elektroniki-ams-9050-kitaj-$) {
        rewrite ^ /catalog/blok-ehlektroniki-ultra-exit-ams-9050 permanent;
	}
    if ( $request_uri ~ ^/catalog/videokamera-ds-t101$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t101 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-t201$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t201 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-t303$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t303 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-t100$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t100 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-t200$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t200 permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-t300$) {
        rewrite ^ /catalog/videokamera-hiwatch-ds-t300 permanent;
	}
    if ( $request_uri ~ ^/catalog/dahua-dhi-xvr4232an-s2$) {
        rewrite ^ /catalog/videoregistrator-dahua permanent;
	}
    if ( $request_uri ~ ^/catalog/gvozdik_AM$) {
        rewrite ^ /catalog/gvozdik-igla-pencil-tag-16-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/gvozdik_pencil_tag_19$) {
        rewrite ^ /catalog/gvozdik-igla-pencil-tag-19-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/gvozdik-igla-super-tag$) {
        rewrite ^ /catalog/gvozdik-igla-dlya-datchikov-supertag permanent;
	}
    if ( $request_uri ~ ^/catalog/gvozdik-igla-dlina-16-mm-$) {
        rewrite ^ /catalog/gvozdik-igla-16-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/gvozdik-igla-dlina-19-mm-$) {
        rewrite ^ /catalog/gvozdik-igla-19-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_gripper$) {
        rewrite ^ /catalog/datchik-spider-pauk permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_supertagmini$) {
        rewrite ^ /catalog/datchik-akustomagnitnyj-mini-super-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_supertag_ii$) {
        rewrite ^ /catalog/datchik-akustomagnitnyj-super-tag-iii permanent;
	}
    if ( $request_uri ~ ^/catalog/pad_pro$) {
        rewrite ^ /catalog/deaktivator-akustomagnitnyj-low-profile-pad-pro permanent;
	}
    if ( $request_uri ~ ^/catalog/amb-2011$) {
        rewrite ^ /catalog/deaktivator-akustomagnitnyj-sensormatic-amb-2011 permanent;
	}
    if ( $request_uri ~ ^/catalog/deaktivator_am_stertec$) {
        rewrite ^ /catalog/deaktivator-akustomagnitnyj-beskontaktnyj-amv-2011 permanent;
	}
    if ( $request_uri ~ ^/catalog/deaktivator_so_zvukom$) {
        rewrite ^ /catalog/deaktivator-radiochastotnyj permanent;
	}
    if ( $request_uri ~ ^/catalog/sajfer-universal-nyj-vnutrennij-204x155x85-mm-standartnyj-zamok-58-kgc-i-8-2-mgc$) {
        rewrite ^ /catalog/zashchitnyj-boks-sajfer-universalnyj-204-155-85 permanent;
	}
    if ( $request_uri ~ ^/catalog/sajfer-universal-nyj-vnutrennij-205x110x85-mm-standartnyj-zamok-58-kgc-i-8-2-mgc$) {
        rewrite ^ /catalog/zashchitnyj-boks-sajfer-universalnyj-205-110-85 permanent;
	}
    if ( $request_uri ~ ^/catalog/obzornoe-zerkalo$) {
        rewrite ^ /catalog/zerkalo-obzornoe-krugloe-410-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/obzornyie_zerkala$) {
        rewrite ^ /catalog/zerkalo-obzornoe-krugloe-610-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/super-lock$) {
        rewrite ^ /catalog/magnitnyj-syomnik-universalnyj-super-lock permanent;
	}
    if ( $request_uri ~ ^/catalog/schetchik_posetiteley_megacount_wi-fi-counter$) {
        rewrite ^ /catalog/provodnoj-komplekt-schetchikov-mc permanent;
	}
    if ( $request_uri ~ ^/catalog/rf550$) {
        rewrite ^ /catalog/radiochastotnaya-antikrazhnaya-sistema-rf-550 permanent;
	}
    if ( $request_uri ~ ^/catalog/rf600$) {
        rewrite ^ /catalog/radiochastotnaya-antikrazhnaya-sistema-rf-600 permanent;
	}
    if ( $request_uri ~ ^/catalog/mono-rf-700$) {
        rewrite ^ /catalog/radiochastotnaya-mono-sistema-rf-700 permanent;
	}
    if ( $request_uri ~ ^/catalog/evolve_p10$) {
        rewrite ^ /catalog/radiochastotnaya-sistema-checkpoint-evolve-p10 permanent;
	}
    if ( $request_uri ~ ^/catalog/trend_checkpoint$) {
        rewrite ^ /catalog/radiochastotnaya-sistema-checkpoint-trend permanent;
	}
    if ( $request_uri ~ ^/catalog/can_tag$) {
        rewrite ^ /catalog/radiochastotnyj-banochnyj-datchik-can-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_butyilochnyiy$) {
        rewrite ^ /catalog/radiochastotnyj-butylochnyj-datchik-bottle-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-flat-tag-s-gvozdem$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-flat-tag-s-gvozdem permanent;
	}
    if ( $request_uri ~ ^/catalog/coffee_tag$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-milk-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_mini_square$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-mini-squaer permanent;
	}
    if ( $request_uri ~ ^/catalog/pencil_tag$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-pencil-tag-midi permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-yurta-25-mm-$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-yurta-30-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-radiochastotnyj-pencil-tag-midi-chernyj-$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-birka-pencil-tag-midi permanent;
	}
    if ( $request_uri ~ ^/catalog/rch-optikal-taq$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-dlya-ochkov-optical-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik_belltag$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-midi-golf-gladkij-54-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/jestkiy_datchik_golftag$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-mini-golf-gladkij-45-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/datchik-radiochastotnyj-midi-golf-kanavki-53-mm-$) {
        rewrite ^ /catalog/radiochastotnyj-datchik-rakushka-midi-golf-53-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h116g$) {
        rewrite ^ /catalog/registrator-ds-h116g permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h216q$) {
        rewrite ^ /catalog/registrator-ds-h116q permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h104g$) {
        rewrite ^ /catalog/registrator-ds-h104g permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h204q$) {
        rewrite ^ /catalog/registrator-ds-h104q permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h108g$) {
        rewrite ^ /catalog/registrator-ds-h108g permanent;
	}
    if ( $request_uri ~ ^/catalog/ds-h208q$) {
        rewrite ^ /catalog/registrator-ds-h108q permanent;
	}
    if ( $request_uri ~ ^/catalog/sistema-radiochastotnaya-rf-550-blak-$) {
        rewrite ^ /catalog/sistema-radiochastotnaya-rf-550 permanent;
	}
    if ( $request_uri ~ ^/catalog/schetchik_posetiteley_smcounter$) {
        rewrite ^ /catalog/schetchik-posetitelej-sm-counter permanent;
	}
    if ( $request_uri ~ ^/catalog/syemniki_golf$) {
        rewrite ^ /catalog/syomnik-magnitnyj-usilennyj-golf permanent;
	}
    if ( $request_uri ~ ^/catalog/syemnik_mk_225$) {
        rewrite ^ /catalog/semnik-mekhanicheskij-mk-225 permanent;
	}
    if ( $request_uri ~ ^/catalog/syemnik_Sensormatic_mk_225$) {
        rewrite ^ /catalog/semnik-mekhanicheskij-sensormatic-mk-225 permanent;
	}
    if ( $request_uri ~ ^/catalog/deactivator_optical_tag$) {
        rewrite ^ /catalog/syomnik-mekhanicheskij-optical-tag permanent;
	}
    if ( $request_uri ~ ^/catalog/trosik_petlja-igla$) {
        rewrite ^ /catalog/trosik-petlya-igla-170-mm permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketka_radiochastotnaya_kruglaya$) {
        rewrite ^ /catalog/ehtiketka-radiochastotnaya-kruglaya-belaya permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketka-belaya-rch-deakt$) {
        rewrite ^ /catalog/ehtiketka-radiochastotnaya-kvadratnaya-belaya permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketki_40x40_mm_lucatron$) {
        rewrite ^ /catalog/ehtiketka-radiochastotnaya-kvadratnaya-shtrih-kod permanent;
	}
    if ( $request_uri ~ ^/catalog/etiketka-kruglaya-rch-deakt$) {
        rewrite ^ /catalog/ehtiketka-radiochastotnaya-kruglaya-shtrih-kod permanent;
	}
    if ( $request_uri ~ ^/sjemnik-zhestkih-datchikov$) {
        rewrite ^ /category/sjemnik-zhestkih-datchikov permanent;
	}

#news

    if ( $request_uri ~ ^/novosti$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cnoviy_ofis_kompanii_axioma$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cfakty$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/csneaker$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/csimalend$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cmagnit_kosmetic$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/ckameri_videonabludenija$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/csportdepo-ekb$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cmagazin_elit_serov$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cbegemotik$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cprikaz_ot_01122014_372$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cprikaz_ot_23052014_153$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/ckachkanarchkij_magazin_vostorg$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cmostovoj$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cnam_5_let$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cmarche_da_roma$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cts_elect$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cvosmerka$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/csityi-dvor$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/ccto$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/caxioma_forever$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cleroy_merlin$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/ctonel$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cokey$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/cleo-ventoni-vybiraet-effektivnuyu-ohrannuyu-sistemu-ultra-shield$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/c-aksioma-sobrala-statistiku-po-vorovstvu-v-rossijskih-magazinah$ ) {
        rewrite ^ / permanent;
    }
    if ( $request_uri ~ ^/c-aksioma-otkryla-tretij-filial-v-krasnodare$ ) {
        rewrite ^ / permanent;
    }


